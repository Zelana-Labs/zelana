ZL_DB_PATH=./zelana-db

ZL_API_HOST=127.0.0.1
ZL_API_PORT=8080

SOLANA_RPC_URL=http://127.0.0.1:8899
SOLANA_WS_URL=ws://127.0.0.1:8900/

ZL_BRIDGE_PROGRAM=

ZL_VERIFIER_PROGRAM_ID=

ZL_DOMAIN=zelana

ZL_MOCK_PROVER=false

ZL_SETTLEMENT_ENABLED=true

ZL_SETTLEMENT_RETRIES=5

BATCH_MAX_TXS=5
BATCH_MAX_AGE=30
BATCH_MAX_SHIELDED=1

# Feature Flags
# Enable dev mode (testing endpoints: /dev/deposit, /dev/seal, /dev/state)
DEV_MODE=true

# Enable fast withdrawals (requires LP setup)
# FAST_WITHDRAW_ENABLED=true

# just intrigued will work on this in future
# Enable threshold encryption for MEV resistance
# THRESHOLD_ENABLED=true
# THRESHOLD_K=2
# THRESHOLD_N=3
# THRESHOLD_DEV=true

# Logging
RUST_LOG=info,zelana_core=debug
